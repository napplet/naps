# Plan 01-01: Build Rename Inventory and Replacement Policy

## Objective

Identify every actionable stale vocabulary surface before editing.

## Tasks

- [x] Check tracked default-branch document content for stale public protocol vocabulary.
- [x] Check tracked default-branch filenames for stale naming.
- [x] Query GitHub issue state.
- [x] Query open PR titles, bodies, branch names, and changed files.
- [x] Classify compatibility-sensitive public protocol strings.

## Findings

- Default branch content matches were found in 5 tracked files: `README.md`, `SPEC.md`, `CLAUDE.md`, `TEMPLATE-WORD.md`, and `TEMPLATE-NN.md`.
- No tracked default-branch filenames currently use stale naming.
- GitHub issues are disabled for the repository, so issue verification is empty by repository configuration.
- 18 open pull requests have stale title vocabulary.
- Open PR changed documents include proposal files and README updates; all require branch-content cleanup.
- The legacy runtime capability prefix is public protocol vocabulary and is in scope for migration to NAP terminology.

## Verification

- Local tracked-content inventory completed with ripgrep.
- Local tracked-filename inventory completed with `git ls-files`.
- GitHub issue query returned the disabled-issues response.
- Open PR metadata and changed-file inventories completed with `gh pr list` and `gh pr view`.
